apiVersion: v1
kind: Service
metadata:
  name: vue-frontend-service
  namespace: app-demo

spec:
  type: NodePort

  selector:
    app: vue-frontend

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
